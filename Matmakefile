# Matmake file
# https://github.com/mls-m5/matmake

cppflags +=                 # c++ only flags
cflags +=                   # c only flags

config += c++17 Wall
config += debug

main.includes +=
    include

main.src =
    src/*.cpp
    # multi line values starts with whitespace

main.copy +=
   data/*.txt
